# DAY5的核心目标
让 Agent 在工具出错、网络抖动、接口超时、结果异常时，不是直接崩溃，而是能重试、降级、回退、记录错误并尽量保证流程继续。
# 真实企业场景常见问题（工具）
    1. 网络抖动
    2. API 超时
    3. 第三方接口异常
    4. 工具返回空结果
    5. 工具返回格式不符合预期
    6. 工具不可用
    7. 某个工具失败但有备用工具
#  处理逻辑
    工具调用前：设置超时、重试策略
    工具调用中：失败后指数退避重试
    工具失败后：尝试备用工具
    工具调用后：统一返回结构

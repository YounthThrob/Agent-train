# DAY6的核心目标
让 Agent 从“一次性执行系统”升级为“有记忆、有上下文、有历史状态的持续系统”。

# 设计思路
User
 ↓
Memory Load
 ↓
Planner
 ↓
DynamicDAGEngine
 ↓
Validator / Repair / ToolRouter
 ↓
Memory Save
 ↓
Final

# 设计目标
## Short-term Memory：短期记忆
当前一次任务中的上下文。

例如：
用户输入
当前参数
当前计划
工具结果
节点 trace

你现在的 AgentState 本质上就是短期记忆。

## Long-term Memory：长期记忆
跨多次请求保存的信息。

例如：
用户常用城市：上海
用户常用报销类型：酒店
用户偏好：回答简洁
用户历史报销记录

## Execution Memory：执行记忆
保存历史任务执行过程。

例如：
上一次请求是什么
执行了哪些节点
哪些工具失败过
是否使用 fallback
最终结果是什么

这对调试和审计很重要。

## Memory.py——用来做长期记忆和执行记忆的管理
load：任务开始前加载记忆
save：任务结束后保存记忆
update_preferences：记录常用城市/报销类型
append_history：记录执行历史

## 总结
建议你始终按这三层理解：

短期记忆：AgentState
长期记忆：memory.json / database
执行记忆：trace / history